import React, { FC } from 'react';

export const Projects: FC = () => (
  <div
    style={{
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      height: '100vh',
    }}
  >
    Projects
  </div>
);
